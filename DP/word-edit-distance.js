/**
 * 单词通过增删改变为另一个单词的最少操作，每一次操作改动一个字符
 * @param {string} from
 * @param {to} to
 */
exports.minEditDistance = function (from, to) {
  if (!from || !to) throw new TypeError('both word length required')

  

}